programa
{
	
	funcao inicio()
	{
		cadeia senha
		
		escreva("escreva uma senha de dificl acesso: ")
		leia(senha)
		
		se(senha == "12345" ou senha == "admin" ou senha == "senha" ou senha == "password" ){
			escreva("essa senha ẽ mt facil")
		}senao{
			escreva("okay, senha legal")
		}
		
	}
}
